# Motor Fault Diagnosis Project

This project focuses on the detection and classification of motor faults (electrical and mechanical) using diagnostic signal fusion. By analyzing current and rotational data, the system identifies the healthy state of the unit as well as various types of induced damages. The analysis includes both the initial model development phase and subsequent verification on independent measurement sets.

---

### Project Structure
```text
.
├── Motor-Fault-Stacking.ipynb              # Main analysis, stacking ensemble training, and model strategy evaluation
├── Diagnostic-Verify.ipynb                 # External validation, temporal stability analysis, and 20kHz vs 10kHz robustness testing
├── Feature-Extractor-With-Resampling.ipynb # Signal preprocessing, feature engineering, and downsampling operations
└── data/                                   # Directory for CSV datasets
    ├── motors.csv                          # Primary training dataset (20kHz)
    ├── temporal_stability_results.csv      # Temporal stability metrics (generated by Diagnostic-Verify)
    ├── results_comparative_performance.csv # Performance comparison matrix (generated by Motor-Fault-Stacking)
    ├── unit_level_results.csv              # External validation results (generated by Diagnostic-Verify)
    ├── resampling_results.csv              # 20kHz vs 10kHz robustness metrics (generated by Diagnostic-Verify)
    ├── Faulty-*.csv                        # New test measurements (20kHz)
    ├── Healthy-*.csv                       # New test measurements (20kHz)
    └── *-10khz.csv                         # Downsampled test sets used for robustness verification
